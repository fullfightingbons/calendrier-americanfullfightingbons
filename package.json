{
	"name": "calendrier-americanfullfightingbons",
	"description": "Calendrier des cours, stages et inscriptions du club AFFBC — Worker Cloudflare + D1, sert calendrier.americanfullfightingbons.fr.",
	"private": true,
	"devDependencies": {
		"typescript": "^5.9.3",
		"vitest": "^2.1.9",
		"wrangler": "4.97.0"
	},
	"scripts": {
		"cf-typegen": "wrangler types",
		"typecheck": "tsc --noEmit",
		"check": "npm run typecheck && npm test && wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
		"dev": "npm run seedLocalD1 && wrangler dev",
		"predeploy": "wrangler d1 migrations apply DB --remote",
		"seedLocalD1": "wrangler d1 migrations apply DB --local",
		"test": "vitest run"
	}
}
